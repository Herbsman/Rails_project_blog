<h1>A list of articles</h1>

<ul id="articles">
	<% @articles.each do |article| %>
		<li><%= link_to article.title, article_path(article), class: "article_title" %></li>
	<% end %>
</ul>
<%= link_to "Write a New Article", new_article_path, class: "new_article" %>