<h1>List of Article Tags</h1>

<ul id="tags">
	<%@tags.each do |tag| %>
	<li> <%= link_to "Delete Tag", tag_path(tag), method: :delete, data: {confirm: "Really delete tag?"}%> | <%= link_to tag.name, tag_path(tag) %></li>
	<% end %>
</ul>

<%= link_to "<<- Back to Articles List", articles_path %>